---
type: ponytail_audit
date: 2026-07-01
targets: nova-vault, nova-dashboard
mode: full
inspector: syzygy
---

# Ponytail Audit — 2026-07-01

Audit of over-engineering across both repos. Tagged findings ranked biggest cut first.

## NovaHub Dashboard (`/home/bo/Downloads/workspace-fa3be643-19a5-48e8-973b-897d6096699a`)

```
shrink  7 route files (agents, services, milestones, timeline, daily-logs, +2 id PATCHes) 
        are byte-for-byte copies varying only by model name. → 1 generic factory.         ~85 lines saved

shrink  4 "shared" components with exactly 1 consumer each (StatCard, AgentQuickCard, 
        ServiceQuickCard, ProgressCard) → inline into command-center.tsx.                  ~120 lines saved

shrink  Status mappings duplicated 3 ways: StatusIcon switch (status→icon), 
        StatusBadge.variants (status→color), StatusBadge.labels (status→text), 
        + agentAccents in agents-view.tsx. → 1 config map.                                ~30 lines saved

stdlib  Custom DatabaseIcon SVG in system-view.tsx:27 when lucide-react has it.            ~12 lines saved

yagni   AnimatedCard wrapper exists but agents-view.tsx and system-view.tsx 
        write inline motion.div config instead. → reuse AnimatedCard.                      ~15 lines saved

shrink  Seed route has 4 near-identical for...of db.create() loops. → 1 helper.            ~15 lines saved

yagni   agentIcons in agents-view.tsx couples UI to specific agent names from seed data.   design debt
```

## Nova Vault (`/home/bo/nova-vault`)

```
shrink  README ~60% overlap with Manifesto, Vault_Index, WORKLOG.                         ~40 lines trim

delete  Pre-completed list in Nova_Timeline.md duplicates checkmarks already in weeks.     ~13 lines

shrink  Syzygy_1.0.md repeats mood/tunnel tables word-for-word from Manifesto.            ~30 lines

shrink  WORKLOG outcomes duplicate commit messages and file deltas from git log.           ~60 lines trim

yagni   Manifesto mood/tunnel tables duplicate dedicated files in _moods/ and _tunnels/.   design debt
```

## Summary

| Metric | Value |
|---|---|
| Findings | 13 |
| Net lines removable | ~435 |
| Dependencies removable | 0 (all in use, but bloat suggests audit) |
| Safety-critical findings | 0 |
| Lean files | tier_build.md, tunnel_index.md, tunnel_build.md, api route helpers (layout/prisma) |

"Lean already. Ship." — tier_build.md, tunnel_index.md, all 5 mood tiers, all 6 tunnel scopes.
